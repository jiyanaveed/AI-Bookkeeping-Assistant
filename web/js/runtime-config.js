/**
 * API origin for browser calls. Empty string = same origin as the page (local FastAPI).
 * On Vercel, `npm run vercel-build` overwrites this file using env `PUBLIC_API_BASE`.
 */
export const API_BASE = "";
